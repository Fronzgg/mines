# 🚀 Деплой на Render.com

## Быстрый деплой

### 1. Подготовка репозитория

Убедитесь, что все файлы закоммичены в Git:

```bash
git add .
git commit -m "Ready for deploy"
git push origin main
```

### 2. Создание Web Service на Render

1. Зайдите на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите ваш GitHub репозиторий
4. Настройте параметры:

**Build Command:**
```
yarn install
```

**Start Command:**
```
node server.js
```

**Environment:**
- Node

### 3. Переменные окружения

Добавьте в Render Dashboard → Environment:

```
PORT=10000
NODE_ENV=production
```

### 4. Настройка для WebSocket

Render автоматически поддерживает WebSocket, но убедитесь, что в `index.html` используется правильный протокол:

```javascript
const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
const wsUrl = `${protocol}//${window.location.host}`;
```

Это уже сделано в вашем проекте! ✅

### 5. База данных

⚠️ **ВАЖНО**: На Render файловая система эфемерная (временная). SQLite база будет удаляться при каждом деплое!

**Решения:**

#### Вариант 1: Использовать Render Disk (рекомендуется)

1. В Render Dashboard → вашего сервиса
2. Перейдите в "Disks"
3. Добавьте новый диск:
   - Name: `frnmines-db`
   - Mount Path: `/opt/render/project/src/data`
   - Size: 1 GB (бесплатно)

4. Обновите `server.js`:
```javascript
const db = new sqlite3.Database('./data/frnmines.db', (err) => {
    // ...
});
```

5. Создайте папку `data` локально и добавьте в `.gitignore`:
```
data/
```

#### Вариант 2: Использовать PostgreSQL (для продакшена)

Render предоставляет бесплатный PostgreSQL. Нужно будет переписать код для работы с PostgreSQL вместо SQLite.

### 6. Проверка деплоя

После деплоя проверьте логи:

```
Render Dashboard → вашего сервиса → Logs
```

Должны увидеть:
```
✅ База данных подключена
✅ Таблица users создана
✅ Основатель @fronzgg добавлен
🚀 Сервер запущен на порту 10000
```

### 7. Настройка домена

Render автоматически даст вам домен типа:
```
https://your-app-name.onrender.com
```

Для своего домена:
1. Render Dashboard → Settings → Custom Domain
2. Добавьте ваш домен
3. Настройте DNS записи

### 8. Настройка Telegram Bot

После деплоя обновите Web App URL в вашем боте:

```
@BotFather → /myapps → выберите ваш бот
→ Edit Web App URL → https://your-app-name.onrender.com
```

## 🐛 Решение проблем

### "SyntaxError: missing ) after argument list"

**Причина**: Ошибка синтаксиса в `server.js`

**Решение**: Проверьте, что все скобки закрыты правильно. Используйте IDE с подсветкой синтаксиса.

### "SQLITE_ERROR: no such table"

**Причина**: База данных не инициализирована или удалена

**Решение**: 
1. Настройте Render Disk (см. выше)
2. Или используйте PostgreSQL

### "Port already in use"

**Причина**: Render использует свой порт

**Решение**: Используйте `process.env.PORT`:
```javascript
const PORT = process.env.PORT || 3000;
```

Это уже сделано! ✅

### "WebSocket connection failed"

**Причина**: Неправильный протокол

**Решение**: Убедитесь, что используется `wss://` для HTTPS:
```javascript
const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
```

Это уже сделано! ✅

### "Build failed"

**Причина**: Ошибка установки зависимостей

**Решение**:
1. Проверьте `package.json`
2. Убедитесь, что все зависимости указаны
3. Проверьте логи сборки

## 📊 Мониторинг

### Логи

Render Dashboard → вашего сервиса → Logs

### Метрики

Render Dashboard → вашего сервиса → Metrics

### Алерты

Настройте уведомления о падении сервиса:
Render Dashboard → Settings → Notifications

## 💰 Стоимость

### Free Tier (бесплатно)
- 750 часов в месяц
- 512 MB RAM
- Автоматический sleep после 15 минут неактивности
- Пробуждение при первом запросе (~30 секунд)

### Starter ($7/месяц)
- Без sleep
- 512 MB RAM
- Всегда онлайн

### Standard ($25/месяц)
- 2 GB RAM
- Больше CPU
- Приоритетная поддержка

## 🔄 Автоматический деплой

Render автоматически деплоит при каждом push в main ветку!

Чтобы отключить:
Render Dashboard → Settings → Auto-Deploy → Off

## 🔐 Безопасность

### Environment Variables

Никогда не коммитьте секреты в Git! Используйте Environment Variables в Render Dashboard.

### HTTPS

Render автоматически предоставляет SSL сертификат! ✅

### Firewall

Render автоматически защищает от DDoS атак! ✅

## ✅ Чеклист перед деплоем

- [ ] Все файлы закоммичены
- [ ] `package.json` содержит все зависимости
- [ ] Используется `process.env.PORT`
- [ ] WebSocket использует правильный протокол
- [ ] Настроен Render Disk для БД (опционально)
- [ ] Обновлен Telegram ID основателя
- [ ] Протестировано локально

## 🎉 Готово!

После деплоя ваше приложение будет доступно по адресу:
```
https://your-app-name.onrender.com
```

Обновите этот URL в вашем Telegram боте и наслаждайтесь! 🚀

---

**Создано by @fronzgg**
