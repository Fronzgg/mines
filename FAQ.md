# ❓ FAQ - Частые вопросы и проблемы

## 🐛 Ошибки при запуске

### 1. "SQLITE_ERROR: no such table: users"

**Причина**: База данных не была правильно инициализирована

**Решение**:
```bash
# Вариант 1: Удалите БД и перезапустите
del frnmines.db
npm start

# Вариант 2: Используйте скрипт сброса
reset_db.bat
npm start
```

---

### 2. "Cannot find module 'express'"

**Причина**: Зависимости не установлены

**Решение**:
```bash
npm install
```

---

### 3. "Port 3000 already in use"

**Причина**: Порт уже занят другим приложением

**Решение**:

**Вариант 1**: Остановите другое приложение на порту 3000

**Вариант 2**: Измените порт в `server.js`:
```javascript
const PORT = process.env.PORT || 3001; // Измените на другой порт
```

---

### 4. "WebSocket connection failed"

**Причина**: Сервер не запущен или проблемы с подключением

**Решение**:
1. Убедитесь, что сервер запущен (`npm start`)
2. Проверьте консоль браузера (F12)
3. Проверьте, что нет блокировки файрволом
4. Попробуйте перезагрузить страницу

---

### 5. "EACCES: permission denied"

**Причина**: Нет прав на создание файлов

**Решение**:
- Windows: Запустите командную строку от имени администратора
- Linux/Mac: Используйте `sudo` или измените права на папку

---

## 🔐 Проблемы с доступом

### 6. "Админ-панель не открывается"

**Причина**: Вы не настроены как основатель

**Решение**:
1. Откройте `server.js`
2. Найдите строки 50-52:
```javascript
db.run(`INSERT OR IGNORE INTO users (telegram_id, username, first_name, balance, verified, is_founder) 
        VALUES (?, ?, ?, ?, ?, ?)`, 
        [0, 'fronzgg', 'Founder', 50000, 1, 1]);
```
3. Замените `0` на ваш Telegram ID
4. Замените `'fronzgg'` на ваш username
5. Удалите `frnmines.db` и перезапустите сервер

**Как узнать свой Telegram ID?**
Напишите боту @userinfobot в Telegram

---

### 7. "Доступ запрещен" в админ-панели

**Причина**: В БД вы не отмечены как основатель

**Решение**:
```sql
-- Откройте БД и выполните:
UPDATE users SET is_founder = 1 WHERE telegram_id = ВАШ_ID;
```

Или удалите БД и пересоздайте с правильным ID основателя

---

## 📱 Telegram интеграция

### 8. "Профиль не загружается из Telegram"

**Причина**: Приложение открыто не через Telegram

**Решение**:
- Для тестирования: Откройте через обычный браузер (будет тестовый профиль)
- Для продакшена: Настройте Telegram Bot и откройте через Mini App

---

### 9. "Как настроить Telegram Bot?"

**Решение**:
1. Напишите @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Получите токен
5. Настройте Web App через `/newapp`
6. Укажите URL вашего сервера

Подробнее в `DEPLOY.md`

---

## 🎮 Проблемы с играми

### 10. "Игра не запускается"

**Причина**: Недостаточно баланса или WebSocket не подключен

**Решение**:
1. Проверьте баланс (должно быть минимум 10 FCOINS)
2. Проверьте WebSocket подключение (F12 → Console)
3. Перезагрузите страницу

---

### 11. "Баланс не обновляется"

**Причина**: WebSocket не подключен

**Решение**:
1. Откройте консоль браузера (F12)
2. Проверьте сообщение "✅ WebSocket подключен"
3. Если нет - перезапустите сервер
4. Перезагрузите страницу

---

### 12. "FN-Live блокировка не работает"

**Причина**: Пользователь не в сети

**Решение**:
- FN-Live работает только для онлайн пользователей
- Проверьте онлайн счетчик
- Убедитесь, что пользователь подключен к WebSocket

---

## 💾 Проблемы с базой данных

### 13. "База данных заблокирована"

**Причина**: Другой процесс использует БД

**Решение**:
1. Остановите все запущенные серверы
2. Закройте все программы, работающие с БД
3. Перезапустите сервер

---

### 14. "Промокод не активируется"

**Причина**: Промокод уже использован или не существует

**Решение**:
1. Проверьте правильность ввода (регистр важен!)
2. Проверьте в админ-панели список промокодов
3. Создайте новый промокод через админ-панель

---

### 15. "Данные не сохраняются"

**Причина**: Проблемы с БД или WebSocket

**Решение**:
1. Проверьте, что сервер запущен
2. Проверьте WebSocket подключение
3. Проверьте логи сервера на ошибки
4. Проверьте права на запись в папку

---

## 🌐 Проблемы с деплоем

### 16. "Не работает на Render/Heroku"

**Причина**: Неправильная конфигурация

**Решение**:
1. Убедитесь, что `package.json` содержит правильные скрипты
2. Проверьте переменные окружения
3. Для WebSocket используйте правильный протокол (wss://)
4. Проверьте логи деплоя

---

### 17. "WebSocket не работает на продакшене"

**Причина**: Неправильный протокол или порт

**Решение**:
В `index.html` измените:
```javascript
const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
const wsUrl = `${protocol}//${window.location.host}`;
```

---

## 🔧 Общие вопросы

### 18. "Как изменить начальный баланс?"

**Решение**:
В `server.js` найдите:
```javascript
balance INTEGER DEFAULT 10000
```
Измените `10000` на нужное значение

---

### 19. "Как добавить новую игру?"

**Решение**:
1. Добавьте HTML разметку в `index.html`
2. Добавьте CSS стили
3. Добавьте JavaScript логику
4. Добавьте сохранение результатов в БД через WebSocket

---

### 20. "Как сделать бэкап БД?"

**Решение**:
```bash
# Windows
copy frnmines.db frnmines_backup.db

# Linux/Mac
cp frnmines.db frnmines_backup.db
```

Или настройте автоматический бэкап (см. `DEPLOY.md`)

---

## 📊 Мониторинг

### 21. "Как посмотреть логи?"

**Решение**:
- Логи сервера: смотрите в консоли, где запущен `npm start`
- Логи клиента: F12 → Console в браузере
- Логи PM2: `pm2 logs frnmines`

---

### 22. "Как узнать количество пользователей?"

**Решение**:
```bash
# Откройте SQLite
sqlite3 frnmines.db

# Выполните запрос
SELECT COUNT(*) FROM users;
```

Или через админ-панель → Список пользователей

---

## 🚀 Оптимизация

### 23. "Сервер работает медленно"

**Решение**:
1. Увеличьте ресурсы сервера (CPU, RAM)
2. Настройте кэширование
3. Используйте CDN для статики
4. Оптимизируйте запросы к БД
5. Используйте Redis для сессий

---

### 24. "Много пользователей - сервер падает"

**Решение**:
1. Используйте PM2 для автоперезапуска
2. Настройте кластеризацию Node.js
3. Используйте балансировщик нагрузки
4. Масштабируйте горизонтально

---

## 💡 Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск сервера
npm start

# Запуск с автоперезагрузкой
npm run dev

# Проверка версии Node.js
node --version

# Проверка версии npm
npm --version

# Очистка кэша npm
npm cache clean --force

# Переустановка зависимостей
rm -rf node_modules package-lock.json
npm install

# Просмотр БД
sqlite3 frnmines.db
.tables
.schema users
SELECT * FROM users;
.quit
```

---

## 📞 Все еще не работает?

1. Проверьте все файлы на месте
2. Проверьте версию Node.js (должна быть 14+)
3. Проверьте логи на ошибки
4. Попробуйте пересоздать проект с нуля
5. Напишите @fronzgg в Telegram

---

**Создано by @fronzgg**
