# 🚀 FRNMINES v3.0 - ОНЛАЙН ИГРЫ

## 📋 План обновления

### ✅ Уже сделано в server.js:
1. Таблица fn_live_blocks с таймером
2. Таблица system_settings
3. API для блокировки с длительностью
4. API для снятия блокировки
5. API для технического перерыва
6. API для переключения FN-Live
7. Broadcast системного статуса

### 🔨 Нужно доделать:

#### 1. Клиентская часть FN-Live (index.html)
- [ ] Обработка fn_live_block с таймером
- [ ] Обработка fn_live_unblock
- [ ] Обработка maintenance_mode
- [ ] Обработка system_status
- [ ] Статус FN-Live в профиле (зеленый/красный щит)
- [ ] Таймер обратного отсчета блокировки
- [ ] Блокировка всех игр кроме профиля

#### 2. Админ-панель (index.html)
- [ ] Поле ввода длительности блокировки (минуты)
- [ ] Кнопка снятия блокировки
- [ ] Переключатель технического перерыва
- [ ] Переключатель FN-Live системы
- [ ] Индикатор статуса FN-Live (активна/неактивна)

#### 3. Ежедневный бонус (index.html + server.js)
- [ ] Таблица daily_bonuses в БД
- [ ] API для получения бонуса
- [ ] Проверка 24 часа на сервере
- [ ] Таймер до следующего бонуса
- [ ] Увеличить бонус до 10,000 FCOINS

#### 4. ОНЛАЙН игра 21 (Блэкджек)
- [ ] Таблица blackjack_games в БД
- [ ] Таблица blackjack_players в БД
- [ ] API создания игры
- [ ] API присоединения к игре
- [ ] API действий (взять карту, остановиться)
- [ ] WebSocket события игры
- [ ] UI игры на клиенте
- [ ] Логика диллера
- [ ] Минимум 2 игрока для старта

#### 5. ОНЛАЙН ракета (Lucky Jet стиль)
- [ ] Таблица rocket_games в БД
- [ ] Таблица rocket_bets в БД
- [ ] API создания раунда
- [ ] API ставки
- [ ] API забрать выигрыш
- [ ] WebSocket синхронизация полета
- [ ] Анимация полета наискосок (как Lucky Jet)
- [ ] История последних раундов
- [ ] Автоматический запуск каждые 30 секунд

#### 6. ОНЛАЙН рулетка
- [ ] Таблица roulette_games в БД
- [ ] Таблица roulette_bets в БД
- [ ] API создания спина
- [ ] API ставки
- [ ] WebSocket синхронизация вращения
- [ ] Все видят одно вращение
- [ ] Таймер до следующего спина
- [ ] История результатов

## 📊 Новые таблицы БД

```sql
-- Ежедневные бонусы
CREATE TABLE daily_bonuses (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER,
    claimed_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    amount INTEGER,
    FOREIGN KEY (user_id) REFERENCES users(telegram_id)
);

-- Игры в 21
CREATE TABLE blackjack_games (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    status TEXT, -- waiting, playing, finished
    dealer_cards TEXT, -- JSON
    dealer_score INTEGER,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    started_at DATETIME,
    finished_at DATETIME
);

CREATE TABLE blackjack_players (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    game_id INTEGER,
    user_id INTEGER,
    cards TEXT, -- JSON
    score INTEGER,
    bet_amount INTEGER,
    status TEXT, -- playing, stand, bust, win, lose
    FOREIGN KEY (game_id) REFERENCES blackjack_games(id),
    FOREIGN KEY (user_id) REFERENCES users(telegram_id)
);

-- Онлайн ракета
CREATE TABLE rocket_games (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    multiplier REAL,
    crash_point REAL,
    status TEXT, -- betting, flying, crashed
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    started_at DATETIME,
    crashed_at DATETIME
);

CREATE TABLE rocket_bets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    game_id INTEGER,
    user_id INTEGER,
    bet_amount INTEGER,
    cashout_multiplier REAL,
    win_amount INTEGER,
    FOREIGN KEY (game_id) REFERENCES rocket_games(id),
    FOREIGN KEY (user_id) REFERENCES users(telegram_id)
);

-- Онлайн рулетка
CREATE TABLE roulette_games (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    result_number INTEGER,
    status TEXT, -- betting, spinning, finished
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    started_at DATETIME,
    finished_at DATETIME
);

CREATE TABLE roulette_bets (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    game_id INTEGER,
    user_id INTEGER,
    bet_type TEXT, -- red, black, green, number
    bet_value TEXT,
    bet_amount INTEGER,
    win_amount INTEGER,
    FOREIGN KEY (game_id) REFERENCES roulette_games(id),
    FOREIGN KEY (user_id) REFERENCES users(telegram_id)
);
```

## 🎮 Игровая логика

### Онлайн ракета (Lucky Jet)
1. Каждые 30 секунд новый раунд
2. 10 секунд на ставки
3. Ракета летит наискосок вверх
4. Случайная точка краша (1.0x - 50.0x)
5. Игроки могут забрать в любой момент
6. Все видят одну ракету

### Онлайн рулетка
1. Каждые 60 секунд новый спин
2. 30 секунд на ставки
3. 30 секунд вращение
4. Все видят одно вращение
5. Автоматический расчет выигрышей

### Онлайн 21 (Блэкджек)
1. Минимум 2 игрока для старта
2. Максимум 5 игроков
3. Диллер раздает карты
4. Игроки ходят по очереди
5. Диллер ходит последним
6. Автоматический расчет выигрышей

## 📝 Приоритеты

1. **Высокий**: FN-Live система (почти готова)
2. **Высокий**: Ежедневный бонус с таймером
3. **Средний**: Онлайн ракета
4. **Средний**: Онлайн рулетка
5. **Низкий**: Онлайн 21 (сложная логика)

## 🚀 Этапы реализации

### Этап 1: Доделать FN-Live (30 минут)
- Клиентская часть
- Админ-панель
- Тестирование

### Этап 2: Ежедневный бонус (20 минут)
- БД и API
- Клиентская часть
- Таймер

### Этап 3: Онлайн ракета (1 час)
- БД и API
- Игровая логика
- WebSocket синхронизация
- Анимация Lucky Jet

### Этап 4: Онлайн рулетка (1 час)
- БД и API
- Игровая логика
- WebSocket синхронизация
- Анимация

### Этап 5: Онлайн 21 (2 часа)
- БД и API
- Игровая логика
- WebSocket синхронизация
- UI

## 💡 Рекомендация

Из-за объема работы, предлагаю:
1. Сначала доделать FN-Live систему полностью
2. Добавить ежедневный бонус
3. Потом по одной онлайн игре

Или сделать все сразу, но это будет ОЧЕНЬ много кода!

Что выбираешь?
